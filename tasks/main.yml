---
- include: install.yml
  tags: ['install']

- name: Create wireguard config dir
  file:
    path: "/etc/wireguard"
    state: directory
  register: wg_conf_dir
  tags: ['always']

- include: configure.yml
  tags: ['config']

- include: users.yml
  tags: ['manage-users']
